class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        
        // logic behind this is let say we have num1 all elements are even or all elements are odd then we dont have any problem always return true 

        // now let say we hv both even as well as odd elements then when we subtract odd - even is always odd so we can always form a odd parity nums2 for this case

        return true;

    }
};